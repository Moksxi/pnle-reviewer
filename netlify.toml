[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (/lessons, /flashcards, /exam) work on reload.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
